---
- name: setting up osd-network-verifier image
  hosts: all
  become: yes
  become_method: sudo

  tasks:
  - name: install required packages
    ansible.builtin.package:
      name: docker
      state: present
